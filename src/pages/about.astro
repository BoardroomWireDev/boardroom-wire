---
const API_KEY = import.meta.env.PUBLIC_YOUTUBE_API_KEY ?? '';
const CHANNEL_ID = 'UCthfphsDjHppg9SQv3JTdrg';
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>About · Boardroom Wire</title>
  <meta name="description" content="Boardroom Wire is forensic business analysis of AI companies and tech industry developments — Bloomberg-style depth, PE/banker lens, no hype." />
  <link rel="icon" type="image/png" href="/logo.png" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap"
    rel="stylesheet"
  />

  <style>
    :root {
      --black:       #000000;
      --black-soft:  #0A0A0A;
      --line:        #1A1A1A;
      --white:       #F5F1E5;
      --white-dim:   #888173;
      --gold:        #D4AF37;
      --gold-bright: #F2CB52;
      --display: 'Inter', system-ui, sans-serif;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    html, body {
      background: var(--black);
      color: var(--white);
      font-family: var(--display);
      overflow-x: hidden;
      min-height: 100vh;
    }

    body::before {
      content: "";
      position: fixed; inset: 0;
      pointer-events: none;
      z-index: 1000;
      opacity: 0.04;
      mix-blend-mode: overlay;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.6'/%3E%3C/svg%3E");
    }

    .ticker {
      display: flex;
      align-items: stretch;
      height: 38px;
      border-bottom: 1px solid var(--line);
      background: var(--black);
      position: relative;
      z-index: 10;
    }
    .ticker-label {
      background: var(--gold);
      color: var(--black);
      font-family: var(--display);
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 0.22em;
      padding: 0 18px;
      display: flex;
      align-items: center;
      gap: 8px;
      text-transform: uppercase;
    }
    .ticker-label .pulse {
      width: 6px; height: 6px;
      background: var(--black);
      border-radius: 50%;
      animation: pulse 1.4s ease-in-out infinite;
    }
    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50%      { opacity: 0.3; }
    }
    .ticker-stats {
      flex: 1;
      overflow: hidden;
      position: relative;
      -webkit-mask-image: linear-gradient(90deg, transparent 0, #000 40px, #000 calc(100% - 40px), transparent 100%);
              mask-image: linear-gradient(90deg, transparent 0, #000 40px, #000 calc(100% - 40px), transparent 100%);
    }
    .ticker-track {
      display: flex;
      align-items: center;
      gap: 56px;
      height: 100%;
      width: max-content;
      padding-left: 32px;
      animation: ticker-scroll 45s linear infinite;
    }
    .ticker:hover .ticker-track,
    .ticker:focus-within .ticker-track {
      animation-play-state: paused;
    }
    @keyframes ticker-scroll {
      from { transform: translateX(0); }
      to   { transform: translateX(-50%); }
    }
    .stat {
      font-family: var(--display);
      font-size: 11px;
      letter-spacing: 0.16em;
      color: var(--white-dim);
      text-transform: uppercase;
      font-weight: 500;
      display: flex;
      align-items: baseline;
      gap: 10px;
      white-space: nowrap;
      flex-shrink: 0;
    }
    .stat .v {
      color: var(--white);
      font-weight: 600;
      font-variant-numeric: tabular-nums;
    }
    .stat.live .v.on  { color: var(--gold-bright); }
    .stat.live .v.off { color: var(--white-dim); }
    .stat .sep {
      color: var(--line);
      margin-left: 56px;
    }

    header.top {
      padding: 32px 48px 0;
      position: relative;
      z-index: 5;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
    }
    .logo {
      display: inline-flex;
      align-items: center;
      gap: 14px;
      text-decoration: none;
      user-select: none;
    }
    .logo-icon { height: 40px; width: 40px; flex-shrink: 0; }
    .logo-text {
      font-family: var(--display);
      font-weight: 900;
      font-size: 22px;
      letter-spacing: 0.04em;
      line-height: 1;
      color: var(--white);
    }
    .logo-text .wire { color: var(--gold-bright); }

    .back {
      font-family: var(--display);
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.28em;
      color: var(--white-dim);
      text-transform: uppercase;
      text-decoration: none;
      transition: color 0.18s;
    }
    .back:hover { color: var(--gold-bright); }

    main.about {
      max-width: 720px;
      margin: 0 auto;
      padding: 88px 32px 96px;
      position: relative;
      z-index: 5;
    }
    .eyebrow {
      font-family: var(--display);
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.32em;
      color: var(--gold-bright);
      text-transform: uppercase;
      margin-bottom: 24px;
    }
    .headline {
      font-family: var(--display);
      font-weight: 600;
      font-size: clamp(36px, 5vw, 56px);
      line-height: 1.05;
      letter-spacing: -0.02em;
      margin-bottom: 32px;
    }
    .headline em {
      font-style: italic;
      color: var(--gold-bright);
    }
    .lede {
      font-family: var(--display);
      font-weight: 400;
      font-size: clamp(18px, 2vw, 22px);
      line-height: 1.55;
      color: var(--white);
      margin-bottom: 56px;
      max-width: 560px;
    }
    .body p {
      font-family: var(--display);
      font-size: clamp(16px, 1.5vw, 18px);
      line-height: 1.7;
      color: var(--white-dim);
      margin-bottom: 22px;
    }
    .body strong {
      color: var(--white);
      font-weight: 600;
    }
    .section-h {
      font-family: var(--display);
      font-weight: 700;
      font-size: 14px;
      letter-spacing: 0.22em;
      color: var(--gold);
      text-transform: uppercase;
      margin: 48px 0 18px;
    }
    .body ul {
      list-style: none;
      padding: 0;
      margin: 0 0 28px;
    }
    .body li {
      font-family: var(--display);
      font-size: clamp(16px, 1.5vw, 18px);
      line-height: 1.6;
      color: var(--white-dim);
      padding-left: 24px;
      position: relative;
      margin-bottom: 14px;
    }
    .body li::before {
      content: "";
      position: absolute;
      left: 0; top: 0.7em;
      width: 12px; height: 1px;
      background: var(--gold);
    }
    .body li strong {
      color: var(--white);
      font-weight: 600;
    }

    .find-us-bottom {
      margin-top: 72px;
      padding-top: 40px;
      border-top: 1px solid var(--line);
    }
    .find-us-label {
      font-family: var(--display);
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 0.28em;
      color: var(--white-dim);
      text-transform: uppercase;
      margin-bottom: 24px;
    }
    .socials {
      display: flex;
      gap: 28px;
      align-items: center;
    }
    .socials a {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 32px;
      height: 32px;
      color: var(--white);
      transition: all 0.2s;
      text-decoration: none;
    }
    .socials a:hover {
      color: var(--gold-bright);
      transform: translateY(-2px);
    }
    .socials svg { width: 22px; height: 22px; fill: currentColor; }

    footer.tail {
      border-top: 1px solid var(--line);
      padding: 28px 48px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      font-family: var(--display);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--white-dim);
    }
    footer.tail a {
      color: inherit;
      text-decoration: none;
      transition: color 0.18s;
    }
    footer.tail a:hover { color: var(--gold-bright); }

    @media (max-width: 720px) {
      header.top { padding: 20px 24px 0; }
      .logo-icon { height: 32px; width: 32px; }
      .logo-text { font-size: 18px; }
      .ticker-track { gap: 36px; padding-left: 20px; }
      .stat { font-size: 10px; letter-spacing: 0.1em; gap: 6px; }
      .stat .sep { margin-left: 36px; }
      .ticker-label { font-size: 10px; padding: 0 12px; }
      main.about { padding: 64px 24px 80px; }
      footer.tail { padding: 20px 24px; flex-direction: column; align-items: flex-start; gap: 12px; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0s !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0s !important;
      }
      .ticker-track { animation: none; transform: none; }
    }
  </style>
</head>
<body>

  <div class="ticker">
    <div class="ticker-label">
      <span class="pulse"></span>
      <span>Live Wire</span>
    </div>
    <div class="ticker-stats" aria-label="Live channel statistics">
      <div class="ticker-track">
        <div class="stat">Videos <span class="v" data-stat="videos">—</span><span class="sep" aria-hidden="true">·</span></div>
        <div class="stat">Subscribers <span class="v" data-stat="subs">—</span><span class="sep" aria-hidden="true">·</span></div>
        <div class="stat">Total Views <span class="v" data-stat="views">—</span><span class="sep" aria-hidden="true">·</span></div>
        <div class="stat live">Live <span class="v off" data-stat="live">Off Air</span><span class="sep" aria-hidden="true">·</span></div>
        <div class="stat" aria-hidden="true">Videos <span class="v" data-stat="videos">—</span><span class="sep">·</span></div>
        <div class="stat" aria-hidden="true">Subscribers <span class="v" data-stat="subs">—</span><span class="sep">·</span></div>
        <div class="stat" aria-hidden="true">Total Views <span class="v" data-stat="views">—</span><span class="sep">·</span></div>
        <div class="stat live" aria-hidden="true">Live <span class="v off" data-stat="live">Off Air</span><span class="sep">·</span></div>
      </div>
    </div>
  </div>

  <header class="top">
    <a href="/" class="logo" aria-label="Boardroom Wire — home">
      <img src="/logo.png" alt="" class="logo-icon" />
      <span class="logo-text">BOARDROOM<span class="wire">WIRE</span></span>
    </a>
    <a href="/" class="back">← Home</a>
  </header>

  <main class="about">
    <p class="eyebrow">About</p>
    <h1 class="headline">Forensic analysis of the companies <em>shaping the future.</em></h1>
    <p class="lede">
      Bloomberg-style depth on AI and the tech industry. Strategic breakdowns,
      not product reviews. Frameworks, not hot takes.
    </p>

    <div class="body">
      <p>
        Most AI coverage stops at "this looks cool" or "this looks dangerous."
        Boardroom Wire starts where that coverage ends — with the numbers, the
        structure, and the strategic logic. Who's winning, who's being
        recapitalized in slow motion, what the data rooms look like, and what
        it all means for the next ten years of the industry.
      </p>

      <p class="section-h">Who's writing</p>
      <p>
        <strong>Dakotah Radabaugh</strong> — eight-plus years in private equity
        tech sales, applied here to AI coverage. Background in selling into the
        firms that quietly shape the deals you read about three quarters later.
      </p>

      <p class="section-h">How to read it</p>
      <ul>
        <li><strong>The channel.</strong> Long-form video breakdowns on YouTube — schedule and live status sit in the wire above.</li>
        <li><strong>The reel.</strong> Latest episodes are pinned on the homepage carousel.</li>
        <li><strong>The wire.</strong> Live channel stats and broadcast status update at the top of every page.</li>
      </ul>

      <p class="section-h">The voice</p>
      <p>
        Analytical. Confident. Compressed — every sentence earns its place.
        Documentary tone, evidence-first. The numbers tell the story.
      </p>
    </div>

    <div class="find-us-bottom">
      <div class="find-us-label">Find Boardroom Wire</div>
      <div class="socials">
        <a href="https://x.com/BoardroomWire" target="_blank" rel="noopener" title="X" aria-label="X">
          <svg viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
        </a>
        <a href="https://www.youtube.com/@BoardroomWire" target="_blank" rel="noopener" title="YouTube" aria-label="YouTube">
          <svg viewBox="0 0 24 24"><path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/></svg>
        </a>
        <a href="#" title="Spotify" aria-label="Spotify">
          <svg viewBox="0 0 24 24"><path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.66 0 12 0zm5.521 17.34c-.24.359-.66.48-1.021.24-2.82-1.74-6.36-2.101-10.561-1.141-.418.122-.779-.179-.899-.539-.12-.421.18-.78.54-.9 4.56-1.021 8.52-.6 11.64 1.32.42.18.479.659.301 1.02zm1.44-3.3c-.301.42-.841.6-1.262.3-3.239-1.98-8.159-2.58-11.939-1.38-.479.12-1.02-.12-1.14-.6-.12-.48.12-1.021.6-1.141C9.6 9.9 15 10.561 18.72 12.84c.361.181.54.78.241 1.2zm.12-3.36C15.24 8.4 8.82 8.16 5.16 9.301c-.6.179-1.2-.181-1.38-.721-.18-.601.18-1.2.72-1.381 4.26-1.26 11.28-1.02 15.721 1.621.539.3.719 1.02.42 1.56-.299.421-1.02.599-1.559.3z"/></svg>
        </a>
        <a href="#" title="Apple Podcasts" aria-label="Apple Podcasts">
          <svg viewBox="0 0 24 24"><path d="M12.012 0C5.388.006.005 5.394 0 12.018c-.006 6.628 5.379 12.018 12 12.018 6.626 0 12-5.394 12-12.018C24 5.394 18.626 0 12.012 0zm-.039 4.97a4.493 4.493 0 0 1 4.494 4.494 4.493 4.493 0 0 1-4.494 4.494 4.493 4.493 0 0 1-4.494-4.494 4.493 4.493 0 0 1 4.494-4.494zm-.025 10.41c2.286 0 3.61.39 4.273.86.456.32.748.842.768 1.404.04.948-.276 1.62-.76 1.987-.96.74-2.34.83-3.07.834l-.443.001c-.728-.005-2.108-.094-3.069-.834-.484-.367-.8-1.04-.76-1.987.02-.562.312-1.083.768-1.404.663-.47 1.987-.86 4.273-.86h.02zm-.02-3.197a2.61 2.61 0 0 1 2.61 2.611 2.61 2.61 0 0 1-2.61 2.611 2.61 2.61 0 0 1-2.611-2.611 2.61 2.61 0 0 1 2.611-2.611z"/></svg>
        </a>
        <a href="#" title="LinkedIn" aria-label="LinkedIn">
          <svg viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.063 2.063 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
        </a>
        <a href="#" title="Instagram" aria-label="Instagram">
          <svg viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881z"/></svg>
        </a>
        <a href="#" title="Substack" aria-label="Substack">
          <svg viewBox="0 0 24 24"><path d="M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z"/></svg>
        </a>
      </div>
    </div>
  </main>

  <footer class="tail">
    <span>© 2026 Boardroom Wire</span>
    <a href="/">← Home</a>
  </footer>

  <script define:vars={{ API_KEY, CHANNEL_ID }}>
    const fmt = (n) => {
      n = Number(n);
      if (n >= 1_000_000) return (n / 1_000_000).toFixed(1).replace(/\.0$/, '') + 'M';
      if (n >= 1_000)     return (n / 1_000).toFixed(1).replace(/\.0$/, '') + 'K';
      return n.toLocaleString();
    };
    const setStat = (name, value) => {
      document.querySelectorAll(`[data-stat="${name}"]`).forEach((el) => {
        el.textContent = value;
      });
    };
    const setLive = (live) => {
      document.querySelectorAll('[data-stat="live"]').forEach((el) => {
        if (live === null || live === undefined) {
          el.textContent = 'Off Air';
          el.className = 'v off';
        } else {
          el.textContent = fmt(live) + ' watching';
          el.className = 'v on';
        }
      });
    };
    function renderStats({ videos, subs, views, live }) {
      setStat('videos', videos);
      setStat('subs',   subs);
      setStat('views',  views);
      setLive(live);
    }
    async function fetchStats() {
      if (!API_KEY) {
        renderStats({ videos: '—', subs: '—', views: '—', live: null });
        return;
      }
      try {
        const chRes = await fetch(`https://www.googleapis.com/youtube/v3/channels?part=statistics&id=${CHANNEL_ID}&key=${API_KEY}`);
        const chData = await chRes.json();
        const s = chData.items?.[0]?.statistics;
        if (!s) throw new Error('No channel stats');
        let liveCount = null;
        const sRes = await fetch(`https://www.googleapis.com/youtube/v3/search?part=snippet&channelId=${CHANNEL_ID}&eventType=live&type=video&key=${API_KEY}`);
        const sData = await sRes.json();
        const liveId = sData.items?.[0]?.id?.videoId;
        if (liveId) {
          const vRes = await fetch(`https://www.googleapis.com/youtube/v3/videos?part=liveStreamingDetails&id=${liveId}&key=${API_KEY}`);
          const vData = await vRes.json();
          liveCount = Number(vData.items?.[0]?.liveStreamingDetails?.concurrentViewers ?? 0);
        }
        renderStats({
          videos: fmt(s.videoCount),
          subs:   fmt(s.subscriberCount),
          views:  fmt(s.viewCount),
          live:   liveCount,
        });
      } catch (err) {
        console.warn('[ticker] stats fetch failed:', err);
        renderStats({ videos: '—', subs: '—', views: '—', live: null });
      }
    }
    fetchStats();
    setInterval(fetchStats, 60_000);
  </script>
</body>
</html>
